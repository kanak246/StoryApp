<%= form_with(model: text_story) do |form| %>
  <% if text_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_story.errors.count, "error") %> prohibited this text_story from being saved:</h2>

      <ul>
        <% text_story.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_of_story %>
    <%= form.text_field :name_of_story %>
  </div>

  <div class="field">
    <%= form.label :description_of_story %>
    <%= form.text_field :description_of_story %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
